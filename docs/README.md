# Documentation assets

Put README screenshots and diagrams in **`images/`** (for example `images/demo-ui.png`).

From the repository root `README.md`, reference them like this:

```markdown
![Demo UI](docs/images/demo-ui.png)
```

Git does not track empty folders; `.gitkeep` keeps `images/` in the tree until you add real image files.
